local GUI = game.Players.LocalPlayer.PlayerGui:FindFirstChild("DMT")
if GUI then
	GUI:Destroy()
	print(GUI.Name.." Was already executed, now deleted and executed again! Have fun!")
else
	--gui
	
	--
end
